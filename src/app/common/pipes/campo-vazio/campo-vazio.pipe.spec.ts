import { CampoVazioPipe } from './campo-vazio.pipe';

describe('CampoVazioPipe', () => {
  it('create an instance', () => {
    const pipe = new CampoVazioPipe();
    expect(pipe).toBeTruthy();
  });
});
